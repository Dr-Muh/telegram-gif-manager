services:
  telegram-gif-manager:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - gif-manager-data:/data

volumes:
  gif-manager-data:
